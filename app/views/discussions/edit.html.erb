<%= render "form" %>

<%= link_to "Index",discussions_path,class: 'btn btn-default' %>
<%= link_to "Remove",discussion_path, method: 'delete',confirm: "Are you sure you want to remove discussion "+(@discus.id).to_s+"?",class: 'btn btn-default' %>